# Database credentials
DB_HOST=localhost
DB_USER=your_username
DB_PASSWORD=your_password
DB_DATABASE=climate

# Environment selection (sagrada or cathedral)
CLIMATE_ENV=sagrada

# Optional: MQTT broker (defaults to localhost:1883)
# MQTT_BROKER=localhost
# MQTT_PORT=1883
